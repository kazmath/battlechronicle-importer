{
  "name": "battlechronicle-importer",
  "version": "3.2",
  "scripts": {
    "build": "npx tsc",
    "watch": "npx tsc --watch",
    "start": "node dist/app.js",
    "lockfile-update": "npm i --package-lock-only"
  },
  "devDependencies": {
    "@types/bootstrap": "^5.2.10",
    "typescript": "^5.8.2"
  }
}
